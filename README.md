# BotHunter VK Parser

## Описание

Парсер для **bot.targethunter.ru** с авторизацией через ВКонтакте. 

### Что парсит:
- **Название и ссылка сообщества ВК** (с главной страницы)
- **ID всех пользователей** со страницы /contacts
- **Автоматическая пагинация** (проходит по всем страницам)
- **Сохранение сессии** (не нужно логиниться каждый раз)

---

## Сборка проекта

### 1. Установка зависимостей

```bash
npm install
npm run install:playwright
npm install -g tsx
```

### 2. Настройка

```bash
# Скопируйте файл конфигурации
cp .env.example .env

# Отредактируйте при необходимости
nano .env
```

### 3. Первый запуск (с авторизацией)

```bash
# Запуск с GUI для авторизации через ВК
tsx bothunter-vk-simple.ts
```

При первом запуске:
1. Откроется браузер
2. Парсер попытается найти кнопку входа через ВК
3. Вы авторизуетесь в ВК
4. Сессия автоматически сохранится
5. Начнется парсинг

### 4. Последующие запуски

```bash
# Можно запускать без GUI - сессия уже сохранена
HEADLESS=true tsx bothunter-vk-simple.ts
```

---

## Ключевые особенности

### Сохранение сессии

**Парсер автоматически сохраняет вашу сессию ВК!**

- Сессия сохраняется в папке `browser-session/`
- При следующем запуске авторизация НЕ требуется
- Сессия действует пока не истечет на сайте

### Результаты

Парсер сохраняет два файла:

1. **`bothunter_results.json`** - полные данные:
```json
{
  "community": {
    "name": "Сестричка Наличка | Займ онлайн",
    "url": "https://vk.com/sestrichka_nalichka",
    "identifier": "6900082c7a8b7b62234064a65b"
  },
  "userIds": ["23473519", "51088480", "22170900", ...],
  "totalUsers": 479,
  "timestamp": "2025-11-01T14:30:00.000Z"
}
```

2. **`bothunter_results_ids.txt`** - только ID (по одному на строку):
```
23473519
51088480
22170900
...
```

---

## Настройки конфигурации (.env)

```env
# Режим браузера
HEADLESS=false        # false = с GUI, true = без GUI

# Ограничение страниц
MAX_PAGES=           # Пусто = все страницы, число = ограничение

# Путь к сессии
SESSION_PATH=./browser-session

# Файл результатов
OUTPUT_FILE=results.json
```

---

## Примеры использования

### Парсинг первых 5 страниц с GUI

```bash
MAX_PAGES=5 HEADLESS=false tsx bothunter-vk-simple.ts
```

### Парсинг всех страниц без GUI

```bash
HEADLESS=true tsx bothunter-vk-simple.ts
```

### Сброс сессии (новая авторизация)

```bash
# Удалите папку с сессией
rm -rf browser-session/

# Запустите с GUI для новой авторизации
tsx bothunter-vk-simple.ts
```

---

## Программное использование

```typescript
import { BotHunterVKParser } from './bothunter-vk-simple';

const parser = new BotHunterVKParser({
  baseUrl: 'https://bot.targethunter.ru',
  headless: false,           // С GUI или без
  maxPages: 10,              // Ограничение страниц
  sessionPath: './session',  // Папка для сессии
  outputFile: 'results.json' // Файл результатов
});

await parser.parse();
```

---

## Структура кода

### Основные методы:

- **`init()`** - инициализация браузера с сохраненной сессией
- **`checkAuth()`** - проверка авторизации
- **`loginVK()`** - полуавтоматическая авторизация через ВК
- **`saveSession()`** - сохранение сессии браузера
- **`extractCommunityInfo()`** - извлечение данных о сообществе
- **`extractUserIds()`** - извлечение ID пользователей со страницы
- **`parse()`** - основной процесс парсинга

---

## Частые вопросы (ЧаВо)

### Как войти вручную?

Если кнопка ВК не найдена, парсер даст вам 2 минуты на ручной вход. Просто авторизуйтесь в открывшемся браузере.

### Как узнать, что сессия сохранена?

Проверьте наличие файла `browser-session/state.json`. Если он есть - сессия сохранена.

### Как парсить конкретное количество страниц?

Установите переменную окружения:
```bash
MAX_PAGES=20 tsx bothunter-vk-simple.ts
```

### Парсер не находит ID пользователей?

Проверьте селекторы в методе `extractUserIds()`. Возможно, структура сайта изменилась.

### Как использовать прокси?

```typescript
const context = await browser.newContext({
  proxy: {
    server: 'http://proxy.example.com:8080',
    username: 'user',
    password: 'pass'
  }
});
```

---

## Решение проблем

### "Браузер не инициализирован"

```bash
# Установите Playwright
npm run install:playwright
```

### "Не удается найти кнопку входа через ВК"

1. Запустите с GUI: `HEADLESS=false`
2. Войдите вручную в течение 2 минут
3. Сессия сохранится автоматически

### "Timeout waiting for navigation"

Увеличьте таймаут в коде:
```typescript
timeout: 120000 // 2 минуты вместо 30 секунд
```

---

## Пример вывода консоли

```
🎯 BotHunter VK Parser - Упрощенная версия
==========================================

📝 Настройки:
   Headless: Нет
   Макс. страниц: Все
   Путь сессии: ./browser-session

🚀 Запуск браузера...
✅ Браузер инициализирован
✅ Уже авторизован
📋 Извлечение информации о сообществе...
✅ Сообщество: Сестричка Наличка | Займ онлайн
   URL: https://vk.com/sestrichka_nalichka
📋 Переход на страницу контактов...

📄 Обработка страницы 1...
   Найдено ID: 20

📄 Обработка страницы 2...
   Найдено ID: 20

⚠️ Достигнута последняя страница

💾 Результаты сохранены в: bothunter_results.json
💾 ID пользователей сохранены в: bothunter_results_ids.txt

✅ Парсинг завершен за 45 секунд
📊 Статистика:
   Сообщество: Сестричка Наличка | Займ онлайн
   Всего ID пользователей: 479
👋 Браузер закрыт
```
